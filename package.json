{
  "name": "job-notion-saver",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/icons && cp -R icons/. dist/icons/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@notionhq/client": "^4.0.2",
    "vite": "^7.1.3",
    "vite-plugin-web-extension": "^4.4.5"
  }
}
